import styled from 'styled-components';
import { Typography } from 'antd';

const { Paragraph } = Typography;

export const FormInputLabel = styled(Paragraph)`
  &.ant-typography {
    margin-bottom: 8px;
  }
`;
