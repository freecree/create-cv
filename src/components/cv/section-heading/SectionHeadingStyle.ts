import styled from 'styled-components';
import { Space } from 'antd';
import colors from '../../../colors';

export const SectionHeadingStyle = styled(Space)`
  font-size: 18px;
  color: ${colors.dark};
`;
