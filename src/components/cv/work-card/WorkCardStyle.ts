import { Space } from 'antd';
import styled from 'styled-components';

export const WorkCardStyle = styled.div`
  display: flex;
  flex-direction: column;
`;

export const WorkDescriptionStyle = styled(Space)`
  margin-top: 7px;
`;
